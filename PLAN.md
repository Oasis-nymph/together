# together — 大纲与计划

> 项目名：**together**。一个轻量、可玩、可回放、可嵌套的 **AI 集体系统科学观察平台**。
> 状态：v0.2 —— Phase 1/2 核心功能已实现（见下方进度）。

---

## 实现进度（v0.2）

### 已完成 ✅
- **Phase 1 MVP**：四维纯白空间（3D+时间）、神经元（点击/拖拽/编辑角色与提示词）、连线与关系（类型/方向/权重）、系统状态光谱（简单→无序+自定义）、拓扑预设、时间轴
- **真实 AI 对话**：OpenAI 兼容 + 本地 Ollama，零依赖本地代理；平台只收集消息并标注元信息，取舍交给模型
- **多层级发散记忆库**：个人/共同/群组记忆；归属范围读写；事件自动记录；推论由模型生成（整理记忆）；衰减；注入下一轮提示词；固化上升（个人→群组）
- **涌现观察层**：参与/共识/新颖/分工/关系多样性 + 综合涌现分 + 运行历史回看
- **涌现场景穷举**：4 个角色社会模板；变体生成（角色×状态×拓扑×风格）；批量运行；按涌现分排名；载入画布/查看对话；规模上限自定义
- **嵌套群组**：可嵌套（父组）、成员归属、群组色环、群组记忆含子组可读
- **新人进入体验**：新 AI 进入环境 → 被介绍 → 自我介绍 → 记录印象
- **出生/死亡/复制**：添加/删除/复制神经元
- **项目导出/导入**（JSON 快照）；**GitHub Pages 自动部署**（可视化可用，AI 对话需本地代理）

### 未来（已规划，未实现）
- 完整**时间分支**（分叉 + 反事实对比；现由运行历史与导出部分覆盖）
- **进化选择压力**（按涌现分淘汰/复制；现由场景穷举部分覆盖）
- 可学习边权重 + Hebbian 规则、内部状态（信任/情绪/能量）
- 相变图、规范/语言涌现检测、消融实验
- 全局记忆层、延迟消息

---

## 0. 一句话定位

把 **AI 对话体** 当作神经元拖进一块画布，用 **可定义关系的连线** 连接它们，让它们 **一对多 / 多对多** 地对话、记忆、嵌套、随时间的推移而演化，共同完成一个总任务——并在此基础上 **观察是否有涌现行为**。

它不是"用多个 AI 干活"的工作流工具，而是一个 **面向系统科学的可玩实验室**。

---

## 1. 背景与差异化

### 1.1 现状（已有近亲，无同类）

| 产品 / 项目 | 是什么 | 与我们的差距 |
|---|---|---|
| Relevance AI Workforce | 可视化画布拖 agent 组"AI 团队" | 任务/流水线导向，非神经元多对多，无关系语义与涌现观察 |
| AutoGen Studio（微软） | 拖拽式多智能体、群聊 | 非神经元画布；边无关系语义；不做涌现观察 |
| LangGraph Studio | 图编辑器（节点=agent/函数，边=控制流） | 面向开发者写代码；边是流程非关系；不轻量 |
| Dify / Langflow / Flowise / n8n | 可视化 LLM 工作流 | 单向 DAG 流水线，无多对多对话，无神经元隐喻 |
| ComfyUI agent 节点 | 借用 ComfyUI 节点画布跑 agent | 套壳 hack，非专门产品 |

研究侧（说明"能否涌现"已被关注）：
- **Moltbook**（硅基社会实验）：AI 网络自对话，研究"社会化/涌现是否出现"。
- **Cooperate or Collapse**：LLM 智能体社会里可持续合作是否涌现。
- **Stanford Generative Agents (Smallville)**：25 个 agent 沙盒自发社交。
- 反面证据：*Large Language Models Miss the Multi-agent Mark*（NeurIPS 2025）等，说明涌现有限、易退化。

### 1.2 我们的空位

现成产品都在"**用**多 agent 干活"，没有人做：**神经元隐喻 + 可定义边关系语义 + 以观察涌现为目的 + 轻量**。这正是本项目的差异化。

### 1.3 系统科学内核（产品的"魂"）

观察对象直接对应系统科学核心概念：
- **涌现 (emergence)**：全局秩序从局部互动中自发产生。
- **自组织 (self-organization)**：无中央裁判、无"机械降神"。
- **层级 (hierarchy)**：涌现是分层级的，砌块逐层拼装。
- **反馈 (feedback)**：正/负反馈决定系统落在哪个态。
- **临界/相变 (criticality / phase transition)**：参数跨过临界点，行为突变。
- **演化与适应 (evolution / adaptation)**：达尔文式相对性，没有绝对最优。

---

## 2. 核心概念模型（产品灵魂，先定死）

### 2.1 节点 = 神经元 / 一个 AI 对话体
- **身份**：名称、角色描述、系统提示词（system prompt）、人格口吻。
- **记忆**：
  - 工作记忆（当前上下文窗口）
  - 长期记忆（向量检索 / RAG，**可衰减**——旧的淡出、重要的固化）
  - 情景记忆（自己经历过的事件）
- **内部状态**（Phase 2+）：信任值、能量/活跃度、情绪倾向。
- **能力**：调用 LLM；调用工具（搜索/代码）；元认知（"我不知道 / 我该去问 X"）。

### 2.2 边 = 突触 / 一段可定义的关系
- **方向**：单向 / 双向。
- **关系类型**：指令、批判、总结、投票、竞争、仲裁、耳语、翻译、过滤……（预设库 + 自定义）。
- **关系提示词模板**：把上游输出"当作 ____ 看待"——边不只是管道，而是**变换器**。
- **权重 / 强度**（Phase 2+ 可学习，见 Hebbian 规则）。
- **不对称**：信任、权力（上级→下级）。
- **时间属性**：即时 / 延迟。

### 2.3 消息 = 信号
- 字段：内容、发送者、接收者、时间戳、类型、引用（可指向过去消息）。
- 沿边流动；**多入 → 聚合 → 单出 → 广播**。

> **设计决策（消息聚合交给模型）**：一个神经元可以**同时收到多条消息**。平台不做任何"智能"的聚合逻辑——它只负责把每条消息**标注元信息**（发送者身份、关系类型、边权重、时间戳），原样打包进该节点的下一次 LLM 调用；**如何取舍、加权、合并，由模型自己判断**。身份不同、权重不同只是给模型的先验提示，不是平台的硬规则。

### 2.4 时间与记忆（事件溯源）
- 所有消息为**不可变事件**，追加进日志 → 免费获得：回放、快进、回退。
- **过去**：记忆检索、引用历史、把某节点在时刻 T 的**快照冻结**成"历史人物"供活节点咨询。
- **未来**：延迟消息（"三天后再读"）、给未来自己的留言、**从任意时间点分叉跑替代未来**（反事实）。
- **时间尺度分离**：快边与慢边嵌套，本身即是涌现来源。

### 2.5 群组 = 分形层级（holon）
- **群组本身也是一个节点**（超级神经元）：内部有拓扑，对外只暴露接口节点（代表/发言人）。
- 群组可**任意层嵌套**（组 → 子组 → …），自相似/分形。
- 支持**群组级边**（组对组对话，内部多对多，外部一条边）。
- **边界渗透性**：开放/封闭可调。

### 2.6 一次运行
- 输入一个**总任务**。
- 调度：轮(tick) / 事件驱动 / 异步 三种模式。
- 放电：每个活跃节点 `收信 → 处理 → 输出 → 广播给邻居`。
- 终止：**收敛** 或 达轮数上限。
- **人类干预**：运行中耳语 / 上帝模式（对任意节点塞话）。

### 2.7 涌现 = 观察全局量（不是任何单节点规定的）
- 分工、共识/分化、规范/语言涌现、相变图、信息熵、派系检测、消融实验。

### 2.8 多层级发散记忆库（每个单位都有记忆）
- **每个单位**——从最小单位（单个神经元）到各种大小的群体——都有自己的记忆库；群体记忆随层级嵌套（个体记忆 → 群组记忆 → 更大群组记忆 → 全局记忆）。
- **发散**（associative，不止一条流水账）：记忆分四类，且互相链接、生长——
  - 事件记忆（发生了什么：消息、事件、时间戳）
  - 印象记忆（谁是谁、对方什么样——从互动中形成的画像）
  - 规范/共识记忆（群体收敛出的约定、行话）
  - 推论记忆（从原始事件"发散"出的解读、联想、结论——**由该单位的模型生成**）
- **固化上升**：重要的个体记忆被提升为群组记忆，重要的群组记忆上升为全局记忆（个人经验 → 共同文化）。
- **遗忘衰减**：不被强化的记忆随时间淡出。
- **读写权限（归属范围）**：记忆按"归属范围"读写——A 的个人记忆只有 A 能读；A 与 B 的共同记忆 A、B 都能读；群组记忆由该群组全体成员可读。写入同理。
- **记录、留下、可分享**：平台在系统提示词中**明确要求模型记录**；所有记录**持久化保存**（不会消失）；分享遵循上述归属范围。
- 模型里的一切信息（消息、状态、输出、事件）都会被**写入对应层级的记忆库**。

### 2.9 涌现场景穷举（让涌现自发发生）
- 涌现是自发的，无法被"命令"出来——所以平台要能**穷举/搜索场景空间**：角色集 × 关系拓扑 × 任务 × 初始记忆种子 × 参数。
- 三种搜索策略：**网格扫描**（系统改变参数）、**随机采样**（蒙特卡洛）、**演化生成**（涌现出有趣行为的场景被保留并变异，像"育种"）。
- 批量并行运行（**规模上限由用户自定义**：并行场景数、token 预算、模型选择），每次运行记录**涌现指标**（新颖性、结构形成、共识、分工），按涌现程度给场景排名。
- 用户可以从"涌现场景库"里挑一个观察、回放、继续分叉。

### 2.10 新人进入体验（AI 像人一样进入新环境）
- 一个新 AI 进入环境时，应当像**一个人进入新环境**：
  - **被介绍**：环境把"这里都有谁、当前的规范/共识"（群组记忆）交给它作为初始认知；
  - **有交互**：与已有成员对话、观察、被询问；
  - **有记忆**：把见闻写入自己的记忆库，逐步形成印象与判断。
- 因此"加入"是一个过程（orientation → 交互 → 归属），而不是一瞬间加载完毕。

### 2.11 系统状态模式（简单 → 混沌的光谱）
- 整个系统可以调成几种状态/关系模式，对应《复杂》的"秩序 ↔ 混沌"光谱：
  - **简单**：单波传播，零噪声。
  - **有序**：严格波动 + 极低噪声。
  - **混沌边缘**：波动 + 适度噪声（稳定与创造力的平衡地带）。
  - **混沌**：波动被随机打断 + 高噪声。
  - **无序**：纯随机放电。
  - **自定义**：手动调波动强度与噪声。
- 实现上映射为两个参数：**波动强度**（有序传播 vs 随机重启）与**噪声**（随机放电比例）；后续可扩展到规则严格度、消息温度等。

---

## 3. 功能范围（P0 / P1 / P2）

### P0 — MVP 必须（跑通最小闭环）
- 画布：拖入节点、拖线连接、定方向、平移缩放。
- 节点：角色 + 系统提示 + 选模型/填 API Key。
- 边：方向 + 关系类型（预设若干 + 自定义提示模板）。
- 一次运行：填总任务 → 按轮调度 → 节点互相对话 → 输出对话记录。
- 基础记忆：工作记忆（上下文窗口）。

### P1 — 近期
- **记忆系统**：长期记忆（检索 + 衰减）、情景记忆；**多层级记忆库**（个体 + 群组级，固化上升 + 遗忘）。
- **时间回放**：快进/回退/快照；延迟消息。
- **涌现观察层**：分工/共识/熵等指标面板；网络"放电"动画；同任务换拓扑对比。
- **群组嵌套**：组 = 节点，组内拓扑 + 对外接口。
- **干预**：耳语 / 上帝模式。

### P2 — 远期 / 发散
- **涌现场景穷举**：场景生成器 + 批量运行 + 涌现指标排名（网格 / 随机 / 演化）。
- **新人进入体验**：新 AI 入场的介绍、交互与记忆积累流程。
- **时间分支**：分叉 + 反事实对比。
- **进化**：节点出生/死亡/复制 + 选择压力（接"囚徒困境社群"）。
- **可学习边权重 + Hebbian 规则**（"一起放电的神经元连在一起"）。
- **内部状态**：信任、情绪、能量。
- **相变图**：行为 vs 参数（拓扑/关系/温度/记忆长度）。
- **规范/语言涌现检测**（共享约定、行话）。
- 内置示例网络：辩论法庭、软件公司、市场、生态系统、科研小组。

---

## 4. 技术选型（轻量是硬指标）

| 层 | 选型 | 说明 |
|---|---|---|
| 前端画布 | React + TypeScript + **React Flow (@xyflow/react)** | 节点+连线画布，神经元外观高度可定制、成熟轻量 |
| 前端样式/状态 | Tailwind CSS + Zustand | 轻量、易维护 |
| 后端运行器 | Node.js (TypeScript) 或 Python FastAPI | 消息调度、调用 LLM |
| LLM 接入 | 统一接口层：OpenAI / Anthropic / **本地 Ollama** | 本地模型控成本 |
| 存储 | 事件日志（append-only JSONL / SQLite）+ 向量库（长期记忆） | 事件溯源，先简单后扩展 |
| 形态 | 先 **网页应用**（本地跑）；后 **Tauri 桌面** 或在线部署 | 轻量起步 |

> 原则：MVP 阶段**零数据库、零登录、零云端**，本地 JSON 即可；复杂度按需后补。

---

## 5. 分阶段路线图

### Phase 0 — 概念冻结（当前）
- 确认本文件的概念模型、功能优先级、技术栈。
- 定工作名。

### Phase 1 — MVP：最小闭环
- **目标**：画布拖 2 节点 + 1 边 + 1 总任务，两个 agent 各调一次 LLM 互说一句话并记录。
- **范围**：React Flow 画布、节点/边编辑、关系类型（3–5 个预设 + 自定义）、调度循环、对话记录面板。
- **验收**：本地打开网页 → 建 2 节点连 1 边 → 填任务 → 跑出完整对话记录。

### Phase 2 — 涌现观察层 + 记忆 + 时间
- **目标**：让"观察涌现"落地。
- **范围**：长期记忆 + 衰减、事件回放/快照、涌现指标面板（分工/共识/熵）、网络放电动画、对比实验。
- **验收**：同一任务换拓扑，能肉眼/指标看到行为差异；能回放到任意时刻。

### Phase 3 — 群组嵌套 + 关系语义 + 时间分支 + 进化
- **目标**：把"结构"和"演化"加满。
- **范围**：组=节点嵌套、组级边、边变换器库、延迟消息与分支未来、出生/死亡/进化、Hebbian 权重。
- **验收**：多层群组可运行；能分叉跑替代未来并对比；跑若干"代"能观察到策略/角色变化。

### Phase 4 — 打包与发布
- **目标**：给别人用。
- **范围**：Tauri 桌面或在线部署、实验配置导出(JSON)、示例网络、教程文档。

---

## 6. 里程碑

- **M1**：MVP 可运行（Phase 1 验收通过）。
- **M2**：涌现观察层 + 回放可用（Phase 2）。
- **M3**：嵌套群组 + 时间分支可用（Phase 3）。
- **M4**：发布首个可分享版本（Phase 4）。

---

## 7. 风险与对策

1. **涌现不保证出现** → 定位为"观察/实验平台"而非"保证涌现"；预设多个可复现的示例网络。
2. **成本爆炸**（多对多、多轮放大 token）→ 默认支持本地 Ollama；轮数/并发/上下文长度可限。
3. **LLM 多智能体易退化/失控**（跑飞、复读机）→ 加入"仲裁节点 / 重置 / 收敛判定"机制。
4. **真正的难点是消息调度与收敛判定**（画布有现成库，不是难点）→ Phase 1 先把调度模型做扎实。

---

## 8. 待定问题（Open Questions）

- [ ] 工作名是否用《涌现实验室》？
- [ ] 关系类型的"标准集"具体列哪些？
- [ ] "收敛"如何判定（N 轮无新观点？达到共识阈值？手动停？）
- [ ] 先做纯本地单人，还是需要账号/云端协作？
- [ ] 免费额度与成本模型（是否需要内置 token 统计）？

> 已定：记忆按归属范围读写（个人仅本人、共同记忆成员共享、群组记忆成员共享）；平台指示模型记录、记录持久化、可分享；场景穷举规模上限由用户自定义；**推论/发散记忆由该单位的模型生成**（记忆库只负责存取与检索，不做总结）。
