# together 代码改进清单（按优先级）

> 对应 paper.md §5，每条标注：难度 / 价值 / 涉及文件。

## P0 —— 先做（直接提升"科学性"与可靠性）

1. **涌现指标升级（语义化）**
   - 共识度/新颖性改用**嵌入相似度**（本地 Ollama embedding 或 API embedding，缓存向量），不再用字符二元组 Jaccard。
   - 增加 **LLM-as-judge 指标**：让裁判模型对"结论一致性/分工是否自发/是否跑题"打分（每场景 1 次调用，成本低）。
   - 增加**消融分析**：删除某节点/某边后重跑，对比指标变化（因果信号）。
   - 涉及：`src/core/emergence.ts`（中 / 高）。

2. **调度器韧性**
   - 每节点调用**自动重试**（指数退避，最多 2 次）——本次实验混沌档 2 个 `terminated` 即因无重试。
   - 并发可配置（当前固定 2），并显示**实时 token/调用预算统计**。
   - 涉及：`src/core/scheduler.ts`、`server/server.mjs`（低 / 高）。

3. **任务完成度评测（补上"解题"维度，消除与任务导向系统的空白）**
   - 接入 MLAgentBench 类小任务集（如"用给定 CSV 预测目标列"），运行结束后由评测器打分，与涌现分并列展示。
   - 涉及：新增 `src/core/bench.ts`（高 / 高）。

## P1 —— 记忆与结构

4. **记忆语义检索**
   - 记忆入库时算 embedding；注入提示词时按"语义相似度 × 强度 × 时间衰减"召回 top-k（当前只是按强度取前 6 条）。
   - 增加**记忆强化**：被引用/复述的记忆强度回升。
   - 涉及：`src/core/memory.ts`（中 / 高）。

5. **群组级对话聚合**
   - 群组可指定"代表节点"，组内多轮后再对外发言（组=超级神经元），与嵌套群组打通。
   - 涉及：`src/core/runner.ts`、`store.ts`（高 / 中）。

6. **系统状态光谱扩展**
   - 除投递层外，把状态映射到：温度、每条消息的截断长度、回复必须引用的最少条数（规则严格度）。
   - 涉及：`src/core/runner.ts`（低 / 中）。

## P2 —— 实验能力

7. **场景穷举并行化 + 预算上限**
   - 浏览器内按"用户自定义并发数"并行跑场景（当前顺序执行），总轮数与 token 预算硬上限。
   - 增加**演化育种**：保留高分场景、变异角色/拓扑/提示词再跑（当前只有网格式变体）。
   - 涉及：`src/state/store.ts`、`src/ui/SearchPanel.tsx`（中 / 高）。

8. **重复实验与统计**
   - 一键"同场景跑 N 次"，输出指标均值±方差（当前 5 档×1 次无法做显著性判断）。
   - 涉及：`SearchPanel`（低 / 高）。

9. **时间分支**
   - 从任意运行历史的时间点"分叉"：复制该时刻前的消息与记忆，改参数后跑替代未来，并排对比。
   - 涉及：`store.ts`、`MessageLog`（高 / 中）。

## P3 —— 工程与发布

10. **测试与 CI**
    - 为 `src/core` 补单元测试（拓扑生成、记忆范围、涌现指标、投递过滤）——核心逻辑已与渲染解耦，天然可测。
    - GitHub Actions 加 `npm run build` 与测试步骤。
    - 涉及：新增 `src/core/*.test.ts`（低 / 高）。

11. **进化机制（出生/死亡/选择）**
    - 每 N 轮按"参与度/被引用率"评估节点，淘汰最差、复制最优（已有复制/删除原语，缺选择压力循环）。
    - Hebbian 边权重：被频繁使用的边权重上升（需先实现边的权重随时间演化）。
    - 涉及：`store.ts`、`scheduler.ts`（中 / 中）。

12. **后端化（让在线版也能对话）**
    - 把零依赖代理改造成可部署的 Vercel Function / Cloudflare Worker（同一份转发逻辑），前端配 `VITE_API_URL` 即可让 GitHub Pages 版跑真实对话。
    - 涉及：`server/server.mjs`（中 / 中）。

## 已知重复点（诚实清单）

| 能力 | 与已有工作的关系 |
|---|---|
| 多智能体协作 + 任务完成 | DeepAnalyze / EvoDS / DS-STAR 已覆盖（我们缺评测，见 P0-3） |
| 技能/上下文管理 | EvoDS 已覆盖（我们的记忆库是弱化版，见 P1-4） |
| 工作流搜索 | AlphaFlow / DS-Agent 已覆盖（我们的场景穷举是"观察导向"变体） |
| 以涌现为观察目标 + 系统状态光谱 + 归属范围记忆 + 交互式神经元可视化 | **未见重复，是差异化所在** |
